# For humans and their AIs

This is a Node.js ERP. Extend it by adding a row to `src/catalog.js` and a test. Keep `list` / `add`. Do not commit secrets.

Paste this file and say: “Add a module for X. Follow catalog.js.”
